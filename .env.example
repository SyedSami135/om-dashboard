# Server
PORT=3000

# PostgreSQL (use in development; production often uses DATABASE_URL only)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE_NAME

# Schema and table (optional; if TABLE_SCHEMA is set, queries use "schema"."table")
TABLE_SCHEMA=customer_support
TABLE_NAME=om_dashboard_ai

# Production: your deployed site URL (for canonical URLs, API base, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000
